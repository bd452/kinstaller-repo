# com.bd452.demo

Minimal KPM demo app. Depends on `com.bd452.fbink` and draws to the screen via
FBInk from the dependency package directory.

```text
/mnt/us/kmc/kpm/packages/com.bd452.fbink/bin/<platform>/fbink
```

Build with `./build.sh` in this directory, or run `./build.sh` at the repo root.
